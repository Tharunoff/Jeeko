node_modules/
dist/
build/
.expo/
.expo-shared/
*.log
.env
.env.local
web-build/
.DS_Store
coverage/
*.keystore
credentials.json

# Downloaded build artifacts — these are large (~123MB) and EAS copies
# every non-ignored file into its upload tarball on each build.
*.apk
*.aab
